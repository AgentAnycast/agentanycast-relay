{
  "name": "agentanycast-relay-ui",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "node esbuild.config.js --watch"
  },
  "dependencies": {
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
